# int normalmente é usado para numeros inteiros float para numeros com virgula
n1 = int(input('Digite o número: '))
n2 = int(input('Digite o número: '))
# sinal de + é para adição em um exercicio matemático
s = n1 + n2
print('A soma de {} + {} é igual a {}.'.format(n1, n2, s))
